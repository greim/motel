{
  "name": "motel",
  "version": "0.2.1",
  "description": "data-vacancy observer",
  "main": "lib/index.js",
  "repository": "https://github.com/greim/motel",
  "scripts": {
    "browser-test": "browserify test-app/test-app.js -o test-app/main.js && here -d test-app",
    "lint": "eslint --max-warnings 0 src/*.js",
    "test": "mocha ./src/**/*.js",
    "compile": "cp -r src/ lib/",
    "sanity": "npm run lint && npm test && npm run compile",
    "prepublish": "npm run sanity"
  },
  "keywords": [
    "data-fetching",
    "react",
    "redux"
  ],
  "author": "Greg Reimer <gregreimer@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^16.5.0",
    "eslint": "^6.7.2",
    "eslint-plugin-babel": "^4.1.1",
    "mocha": "^6.2.2",
    "serve-here": "^3.2.0",
    "sinon": "^2.1.0"
  },
  "dependencies": {
    "url-pattern": "^1.0.3"
  }
}
